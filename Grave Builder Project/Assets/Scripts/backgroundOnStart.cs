using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class backgroundOnStart : MonoBehaviour
{

    public AudioSource saw;

    // Start is called before the first frame update
    void Start()
    {
        saw.Play();   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
